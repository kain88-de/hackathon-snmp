[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "traceformat"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["pydantic>=2"]

[dependency-groups]
dev = [
    "datamodel-code-generator>=0.25",
    "pytest>=8",
    "ruff>=0.9",
    "pyrefly>=1.0",
    "pyright>=1.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/traceformat"]

[tool.ruff]
line-length = 100
# models.py is generated by datamodel-code-generator (black/isort formatted);
# ruff must not reformat it or the types-fresh drift check would fail.
extend-exclude = ["src/traceformat/models.py"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "C4", "SIM", "ARG", "RUF", "ASYNC", "TC", "PTH", "PL"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["PLR2004"]

[tool.pyrefly]
project_includes = ["src", "tests"]
search_path = ["src", "."]
python_interpreter = "../.venv/bin/python3"

[tool.pyright]
venvPath = ".."
venv = ".venv"
typeCheckingMode = "strict"
exclude = ["src/traceformat/models.py"]

[tool.pytest.ini_options]
testpaths = ["tests"]
