{
  "name": "oidviz",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "bun test",
    "lint": "bunx oxlint -D all -c .oxlintrc.json --ignore-pattern \"src/lib/types.gen.ts\" -A \"unicorn/filename-case\" -A \"sort-imports\" src/ && bunx biome check --formatter-enabled=false src/",
    "check": "bunx tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.0.0",
    "typescript": "^5.7.0",
    "json-schema-to-typescript": "^15.0.0",
    "@types/bun": "latest"
  }
}
