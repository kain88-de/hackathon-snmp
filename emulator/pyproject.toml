[project]
name = "emulator"
version = "0.1.0"
description = "SNMP device emulator"
requires-python = ">=3.12"
dependencies = [
    "pysnmp>=7.1.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
